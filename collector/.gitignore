# Build output
bin/
*.exe
*.test
*.out

# Go workspace / module cache (local dev only)
go.work
go.work.sum

# Coverage
coverage.out
coverage.html
*.coverprofile

# Config with secrets (use config.example.toml as template)
configs/config.toml
configs/config.local.toml
.env
.env.*
!.env.example

# Logs / temp
*.log
tmp/

# IDE / editor
.idea/
.vscode/
*.swp
*~

# macOS
.DS_Store

# Docker volumes (if mounted into repo)
deploy/data/
